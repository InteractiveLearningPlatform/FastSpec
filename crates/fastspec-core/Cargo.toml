[package]
name = "fastspec-core"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
fastspec-model = { path = "../fastspec-model" }
serde = { version = "1", features = ["derive"] }

[lib]
path = "src/lib.rs"

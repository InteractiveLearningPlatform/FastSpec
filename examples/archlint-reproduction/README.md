# Archlint Reproduction Example

This example shows how FastSpec can describe an app-creation target using compact YAML artifacts.

The example is intentionally static in this slice. It provides a concrete shape for future runtime work:

- a project-level spec
- module-level specs
- enough structure for an agent or future CLI to reason about the target application

## 1. PostgreSQL Corpus Store

- [x] 1.1 Add PostgreSQL dependencies and implement a `CorpusStore` adapter for source documents and chunks
- [x] 1.2 Add schema bootstrap and document replacement behavior for PostgreSQL persistence

## 2. Runtime Wiring And Verification

- [x] 2.1 Add runtime store selection for file vs PostgreSQL storage in the Speclist API bootstrap path
- [x] 2.2 Add tests for PostgreSQL store behavior and runtime selection, then document the new configuration

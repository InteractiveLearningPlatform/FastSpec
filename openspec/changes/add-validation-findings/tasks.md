## 1. Validation model and rules

- [x] 1.1 Add structured validation finding types and result payloads in the core runtime
- [x] 1.2 Implement the first cross-document validation rules for duplicate IDs, project/module mismatches, and invalid internal module dependencies

## 2. CLI validation command

- [x] 2.1 Add a `validate` command to the CLI with human-readable output
- [x] 2.2 Add `--json` support for the validation command using the shared finding payloads

## 3. Verification and docs

- [x] 3.1 Add tests for clean validation, failing validation, and JSON validation output
- [x] 3.2 Update CLI docs to describe the new validation command and its output modes

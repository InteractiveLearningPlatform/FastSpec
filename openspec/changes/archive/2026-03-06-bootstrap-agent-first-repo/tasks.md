---
area: repository
component: bootstrap
kind: tasks
status: active
tags:
  - openspec
  - fastspec
  - bootstrap
---

# Tasks

- [x] 1. Enrich `openspec/config.yaml` with FastSpec-specific context and artifact rules
- [x] 2. Rewrite `README.md` so the repo explains the FastSpec/OpenSpec relationship and the intended layout
- [x] 3. Add focused docs for architecture, OpenSpec workflow, and repository structure
- [x] 4. Add reusable YAML templates for project, module, and agent capability specs
- [x] 5. Add an `examples/archlint-reproduction/` example with realistic FastSpec YAML documents
- [x] 6. Add Rust-oriented `apps/` and `crates/` directory stubs to make the intended runtime shape explicit
- [x] 7. Add basic repo hygiene files such as `.editorconfig` and `.gitignore` updates for local tooling
- [x] 8. Add a minimal Cargo workspace with `fastspec-model`, `fastspec-core`, and `fastspec-cli`
- [x] 9. Add tests for spec-kind detection and example-tree validation
- [x] 10. Validate the OpenSpec change, file layout, and Rust workspace

## 1. Graph model

- [x] 1.1 Add normalized graph node and edge payloads in the core runtime
- [x] 1.2 Build graph export from a validation-clean FastSpec tree using project, module, workflow, and internal dependency relationships

## 2. CLI graph command

- [x] 2.1 Add a `graph` command with human-readable graph output
- [x] 2.2 Add `--json` support for graph export using the shared graph payloads

## 3. Verification and docs

- [x] 3.1 Add tests for successful graph export and invalid-tree rejection
- [x] 3.2 Update CLI docs to describe the graph command and output modes

apiVersion: v1
kind: Service
metadata:
  name: {{ include "speclist-platform.fullname" . }}-api
  labels:
    {{- include "speclist-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
